#include "main.h"
/**
 * _islower - checks if c is lowercase letter
 *
 * @c: character to check
 * Return: 1 always (success)
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
}
