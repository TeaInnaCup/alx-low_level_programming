#include "main.h"
/**
 * main - check code
 *
 * Return: 0 Always (Success)
 */
int main(int argc, char *argv[])
{
	for(i = 0; arg[0][i] != '\0'; i++)
	{
		_putchar(argv[0][i])
	}
	_putchar('\n');
	return (0);
}
