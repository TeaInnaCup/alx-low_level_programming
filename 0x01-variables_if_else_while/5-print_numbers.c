#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 Always (Success)
 */
int main(void)
{
	int number;

	number = 0;
	for (number = 0; number <= 9; number++)
	{
		printf("%d\n", number);
	}
	return (0);
}
